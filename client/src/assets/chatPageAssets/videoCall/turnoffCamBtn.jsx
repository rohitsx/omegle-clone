import { useEffect } from "react"

export default function KillMediaStream({ localVideo, peerConnection, setPc, socket, strangerUserId }) {

    // return <div id="cam">
    //     <button id="camOn" onClick={camOn}>Cam On</button>
    //     <button id="camOff" onClick={camOff}>Cam Off</button>
    // </div>
}